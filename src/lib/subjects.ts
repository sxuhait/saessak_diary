// Quick-pick options for the session log "과목" field. Free text is still
// allowed (via a datalist, not a closed <select>) -- this just keeps the
// common cases spelled consistently so subject-based aggregation doesn't
// fragment on things like "수학" vs "수학 " vs "Math".
export const COMMON_SUBJECTS = ["수학", "영어", "국어", "독서", "사회", "과학"];
